require 'rails_helper'

RSpec.describe JointsController, :type => :controller do

  it 'should have a test that passes' do
    expect(1).to eq(1)
  end

  it 'should have a test that fails' do
    expect(:one).to eq(2)
  end
end
